{
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@types/bun": "latest",
    "ultracite": "7.7.0"
  },
  "name": "wsync",
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "start": "bun run src/index.ts start",
    "start:dry-run": "bun run src/index.ts start --dry-run",
    "start:debug": "bun run src/index.ts start --debug",
    "dev": "bun --watch run src/index.ts start",
    "status": "bun run src/index.ts status",
    "stop": "bun run src/index.ts stop",
    "typecheck": "tsc --noEmit",
    "fix": "ultracite fix"
  },
  "type": "module",
  "version": "0.1.0",
  "description": "Clockify -> Jira Time Sync",
  "module": "src/index.ts",
  "private": true
}
